# UrbanStream Python dependencies
# pip install -r requirements.txt

# Kafka producer
confluent-kafka>=2.3.0

# Data / ML
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyspark>=3.5.0

# Storage + cache
# NOTE: s3fs removed — Spark accesses MinIO via hadoop-aws JAR (bundled at spark-submit time)
# boto3 kept only for download_data.py; not used in the hot path
redis>=5.0.0
boto3>=1.34.0

# Dashboard
streamlit>=1.32.0
pydeck>=0.8.0
plotly>=5.18.0
folium>=0.16.0
streamlit-folium>=0.18.0

# Utilities
requests>=2.31.0
python-dateutil>=2.8.0
